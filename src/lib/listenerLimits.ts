/** Max realtime chat threads per user (inbox + Synq tab). */
export const CHATS_LISTENER_LIMIT = 60;

/** Max activity notifications in realtime listeners. */
export const NOTIFICATIONS_LISTENER_LIMIT = 80;

/** Max friend requests shown in realtime listeners. */
export const FRIEND_REQUESTS_LISTENER_LIMIT = 40;
